:8080 {
    encode gzip zstd

    # Proxy backend traffic to the api container on the compose network.
    handle /api/* {
        reverse_proxy api:4000
    }

    # SPA fallback for everything else.
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
